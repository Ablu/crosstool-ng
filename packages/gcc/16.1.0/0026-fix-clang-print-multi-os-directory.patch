From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Ahmed ARIF <arif.ing@outlook.com>
Date: Mon, 12 Jan 2026 00:00:00 +0000
Subject: [PATCH] gcc: fix clang print-multi-os-directory fallback

When using clang as the host compiler, -print-multi-os-directory may
fail. Add error handling to default to "." when the command fails.

Index: gcc-16.1.0/libgfortran/configure
===================================================================
--- gcc-16.1.0.orig/libgfortran/configure
+++ gcc-16.1.0/libgfortran/configure
@@ -5213,7 +5213,10 @@ case ${version_specific_libs} in
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
@@ -12132,6 +12135,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
Index: gcc-16.1.0/libstdc++-v3/configure
===================================================================
--- gcc-16.1.0.orig/libstdc++-v3/configure
+++ gcc-16.1.0/libstdc++-v3/configure
@@ -11566,6 +11566,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
@@ -56363,7 +56366,10 @@ fi
       glibcxx_toolexecdir='${libdir}/gcc/${host_alias}'
       glibcxx_toolexeclibdir='${libdir}'
     fi
-    multi_os_directory=`$CXX -print-multi-os-directory`
+    multi_os_directory=`$CXX -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) glibcxx_toolexeclibdir=$glibcxx_toolexeclibdir/$multi_os_directory ;;
Index: gcc-16.1.0/gcc/configure
===================================================================
--- gcc-16.1.0.orig/gcc/configure
+++ gcc-16.1.0/gcc/configure
@@ -20880,6 +20880,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
Index: gcc-16.1.0/libatomic/configure
===================================================================
--- gcc-16.1.0.orig/libatomic/configure
+++ gcc-16.1.0/libatomic/configure
@@ -3406,7 +3406,10 @@ case ${enable_version_specific_runtime_l
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
@@ -10771,6 +10774,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
Index: gcc-16.1.0/libgomp/configure
===================================================================
--- gcc-16.1.0.orig/libgomp/configure
+++ gcc-16.1.0/libgomp/configure
@@ -3518,7 +3518,10 @@ case ${enable_version_specific_runtime_l
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
@@ -10763,6 +10766,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
Index: gcc-16.1.0/libitm/configure
===================================================================
--- gcc-16.1.0.orig/libitm/configure
+++ gcc-16.1.0/libitm/configure
@@ -3582,7 +3582,10 @@ case ${enable_version_specific_runtime_l
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
@@ -11425,6 +11428,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
Index: gcc-16.1.0/libsanitizer/configure
===================================================================
--- gcc-16.1.0.orig/libsanitizer/configure
+++ gcc-16.1.0/libsanitizer/configure
@@ -5073,7 +5073,10 @@ case ${version_specific_libs} in
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
@@ -11769,6 +11772,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
Index: gcc-16.1.0/libquadmath/configure
===================================================================
--- gcc-16.1.0.orig/libquadmath/configure
+++ gcc-16.1.0/libquadmath/configure
@@ -10198,6 +10198,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
@@ -12640,7 +12643,10 @@ case ${version_specific_libs} in
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
Index: gcc-16.1.0/libssp/configure
===================================================================
--- gcc-16.1.0.orig/libssp/configure
+++ gcc-16.1.0/libssp/configure
@@ -10440,6 +10440,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
@@ -11973,7 +11976,10 @@ case ${version_specific_libs} in
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
Index: gcc-16.1.0/libvtv/configure
===================================================================
--- gcc-16.1.0.orig/libvtv/configure
+++ gcc-16.1.0/libvtv/configure
@@ -4861,7 +4861,10 @@ case ${version_specific_libs} in
       toolexecdir='$(libdir)/gcc-lib/$(target_alias)'
       toolexeclibdir='$(libdir)'
     fi
-    multi_os_directory=`$CC -print-multi-os-directory`
+    multi_os_directory=`$CC -print-multi-os-directory 2>/dev/null`
+    if test $? -ne 0 || test -z "$multi_os_directory"; then
+      multi_os_directory="."
+    fi
     case $multi_os_directory in
       .) ;; # Avoid trailing /.
       *) toolexeclibdir=$toolexeclibdir/$multi_os_directory ;;
@@ -11663,6 +11666,9 @@ if test "$GCC" = yes; then
   # and add multilib dir if necessary.
   lt_tmp_lt_search_path_spec=
   lt_multi_os_dir=`$CC $CPPFLAGS $CFLAGS $LDFLAGS -print-multi-os-directory 2>/dev/null`
+  if test $? -ne 0 || test -z "$lt_multi_os_dir"; then
+    lt_multi_os_dir="."
+  fi
   for lt_sys_path in $lt_search_path_spec; do
     if test -d "$lt_sys_path/$lt_multi_os_dir"; then
       lt_tmp_lt_search_path_spec="$lt_tmp_lt_search_path_spec $lt_sys_path/$lt_multi_os_dir"
